# AI Focus Mode Extension

## Modules

* `Module-1`: Setting up extensions  UI -> HTML and css
* `Module-2`:  CSS/Adding interactivity
* `Module 3`: Communication b/w different parts of chrome extension
* `Module 4`: Manually blocking websites
* `Module 5`: Adding AI 

### Module 1
* Extension -> chrome extension, vscode
* json -> data  format
* manifest.json -> this is the file used by  chrome to let it know what kind of functionality / permission your extension will be required
    * You will need to know which keys and values are `key : value -> chrome`
* UI 
    * html file -> show UI
    * css file for -> styling

## Module 2
* pseduo classes : hover , focus , checked -> if else stamenets in css
* transition , transform -> translate
* circle
* how to beatify your inputs 

### JS topic
* variables & datatypes
* if else  -> 
* loops-> 5 questions-> Patterns 
* functions -> 
* arrays  -> 5 question (1d and few of 2d)
* string -> 5 questions
* how to take an input -> Prompt

## Rules 
* code excutes -> left  to right , top to bottom
* default value inside variable is undefined
* Primitives type  : number, string , boolean , null, undefined 


### Module-2 Part-3
* Implementing show and hide functionality
* add and remove sites

## Module-3 & 4 
*  Delete tasks
*  Connecting Different parts of chrome extension
*  Blocking every tabs
*  Blocking only those tabs that are entered by user
   *  we can block a given url 
   *  backgrund script that stay even the frontend closes 
*  How can ai assit while learning new concepts


  * Blocking only those tabs that are entered by user
   *  we can block a given url 
   *  background script that stay even the frontend closes
      *  we need to commnuicate from popup-> background 


* User Frontend -> enter url (popup) -> background -> tab is updated -> blocked url

  https:// -> protocol
  www -> world wide web
  google -> hostname, domain
  .com -> subdomain
  /search -> route
  ?q=hello-> query params




